name: VideoRecord
debug: true
files:
  - name: src/desktopVideoRecord.py
commands:
  - name: init
    command: python -m pip install --upgrade pip && python -m pip install Pillow moviepy opencv-python
